# OIBSIP_Python_Task3 – Random Password Generator

## 🎯 Objective
The objective of this project is to build a **Random Password Generator** in Python that allows users to generate secure passwords with customizable options and automatically copy them to the clipboard.

---

## ⚙️ Steps Performed
1. Collected user input for password length.  
2. Allowed users to choose character types: **Uppercase, Lowercase, Numbers, Symbols**.  
3. Ensured password security (warning if length < 8).  
4. Generated a random password based on selected criteria.  
5. Copied the generated password to the clipboard automatically.  

---

## 🛠️ Tools & Libraries Used
- Python 3.x  
- `random` & `string` – for password generation  
- `pyperclip` – to copy password to clipboard  
- `rich` – for styled CLI interface  

---

## 📂 Project Structure
OIBSIP_Python_Task3/  
│── password_generator.py   # Main Python code  
│── README.md               # Project documentation  
